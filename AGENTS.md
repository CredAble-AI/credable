# AGENTS.md

이 파일은 Codex가 CredAble 저장소의 프론트엔드와 백엔드에서 공통으로 따라야 하는 규칙이다.
모든 작업 전에 이 파일과 [`CONTRIBUTING.md`](CONTRIBUTING.md)를 먼저 읽는다.

## 1. 작업 시작 전

1. `git status --short --branch`로 현재 브랜치와 변경 상태를 확인한다.
2. 저장소 구조, 실행 명령, 테스트 설정과 작업 대상 코드를 먼저 읽는다.
3. 프론트엔드, 백엔드, 공통 계약 중 변경 영역을 구분한다.
4. `main`이라면 파일을 수정하기 전에 새 작업 브랜치를 생성한다.
5. 기존 사용자 변경을 덮어쓰거나 관련 없는 파일을 수정하지 않는다.

## 2. 공통 개발 원칙

- 기존 구조, 언어, 포맷터와 명명 규칙을 우선한다.
- 요청 범위에 필요한 최소 변경을 수행한다.
- 코드 중복보다 명확한 책임 분리를 우선하되 성급한 추상화는 피한다.
- 비밀키, 토큰, 개인정보, 실제 고객 데이터와 로컬 환경 파일을 커밋하지 않는다.
- 오류를 숨기지 않으며 사용자에게 복구 가능한 메시지와 추적 가능한 정보를 제공한다.
- 기능 변경에는 테스트와 필요한 문서 변경을 포함한다.
- 검증되지 않은 수치, 모델 성능과 금융 결과를 만들어내지 않는다.

## 3. 프론트엔드 규칙

- 서버 응답을 신뢰 가능한 원본으로 사용하며 적격성, 위험도, 정책과 경로를 클라이언트에서 임의로 재계산하지 않는다.
- API 호출, 상태 관리, 화면 컴포넌트와 표현 로직을 분리한다.
- 로딩, 빈 상태, 오류, 재시도와 권한 부족 상태를 빠뜨리지 않는다.
- 사용자 입력은 클라이언트와 서버 양쪽에서 검증되는 것으로 가정한다.
- 반응형 레이아웃, 키보드 사용, 포커스, 라벨과 색상 대비를 확인한다.
- UI 변경은 가능하면 스크린샷 또는 재현 절차를 PR에 포함한다.
- API 필드명과 상태를 추측하지 말고 실제 계약과 타입을 확인한다.

## 4. 백엔드 규칙

- API, 서비스, 저장소, 외부 연동과 도메인 규칙의 책임을 분리한다.
- 프론트엔드가 보낸 판단값을 그대로 신뢰하지 않고 정책과 서버 입력으로 재검증한다.
- 핵심 판단에는 감사 로그, 정책·모델·데이터 버전, 입력 스냅샷, 기준시점과 `demoOnly` 상태를 보존한다.
- 데이터가 없다는 이유로 위험도를 임의로 악화시키지 않는다.
- 의심되거나 기준시점 이후의 증빙은 위험도 입력에서 제외한다.
- 외부 연동 실패, Mock·Fixture·Snapshot과 fallback 사용 여부를 명확히 구분한다.
- DB나 스키마 변경에는 마이그레이션, 호환성 및 롤백 영향을 검토한다.

## 5. 프론트엔드·백엔드 계약

- API 변경 전 현재 요청·응답 스키마와 사용처를 모두 찾는다.
- 계약 변경 시 백엔드 스키마, 프론트엔드 타입·호출부, Mock, 테스트와 문서를 함께 갱신한다.
- 필드 삭제·이름 변경·타입 변경은 breaking change로 간주하고 PR에 명시한다.
- 한쪽 구현이 준비되지 않았다면 명시적인 Mock 또는 호환 레이어를 사용하고 임시 상태임을 기록한다.
- 날짜·시간대, nullable 필드, enum, 오류 코드와 HTTP 상태를 양쪽에서 일치시킨다.

## 6. CredAble 도메인 원칙

- CredAble은 추가 증빙을 기반으로 거절·보류 건의 재심사를 지원한다.
- AI는 확정된 구조화 결과를 설명할 뿐 적격성, 증빙 추천, 위험도, 정책과 경로를 생성하거나 변경하지 않는다.
- 최종 금융 판단은 심사역이 수행한다.
- 실제 검증 전에는 모델 성능, 승인 효과, PD·LGD·EAD·EL을 사실처럼 표현하지 않는다.

## 7. 검증

변경 영역에 존재하는 검증 명령을 실행한다.

- 공통: 포맷 검사, 린트, 타입 검사, `git diff --check`
- 프론트엔드: 단위·컴포넌트 테스트, 빌드, 주요 화면 수동 확인
- 백엔드: 단위·API·도메인 규칙 테스트, 마이그레이션 확인
- 양쪽 변경: 실제 또는 Mock API를 이용한 통합 흐름 확인

명령이 없거나 실행할 수 없다면 임의로 성공했다고 하지 말고 미검증 범위와 이유를 적는다.

## 8. GitHub 작업은 필수

파일을 변경하는 모든 작업은 아래 순서를 따른다.

**기능 하나 = 작업 브랜치 하나 = Pull Request 하나**를 원칙으로 한다.
서로 다른 기능이나 수정 목적을 같은 브랜치와 PR에 묶지 않는다.
하나의 기능을 완성하기 위한 프론트엔드·백엔드·API 계약 변경은 같은 브랜치와 PR에 함께 포함할 수 있다.

1. 작업 브랜치 생성 또는 확인
2. 구현 및 검증
3. 작은 논리 단위로 커밋
4. 원격 브랜치로 푸시
5. Pull Request 생성 및 리뷰 요청

브랜치·커밋·PR 중 하나라도 빠지면 완료로 보고하지 않는다.
원격 저장소, 인증 또는 권한이 없어 푸시나 PR 생성이 불가능하면 생략하지 말고 남은 단계와 실행 명령을 사용자에게 알린다.
상세 규칙은 [`CONTRIBUTING.md`](CONTRIBUTING.md)를 따른다.

## 9. 완료 보고

- 프론트엔드·백엔드·공통 중 변경 영역
- 주요 변경 파일과 동작
- 실행한 검증과 결과
- 알려진 제한사항 또는 위험
- 브랜치명, 커밋 해시와 PR 링크

