# CLAUDE.md

이 파일은 Claude Code가 CredAble 저장소의 프론트엔드와 백엔드에서 공통으로 따라야 하는 규칙이다.
작업 전에 이 파일과 [`CONTRIBUTING.md`](CONTRIBUTING.md)를 반드시 읽는다.

## 작업 시작 체크리스트

- [ ] `git status --short --branch`로 현재 상태를 확인했는가?
- [ ] `main`이 아닌 목적이 분명한 작업 브랜치인가?
- [ ] 저장소 구조와 기존 구현·테스트·명령을 먼저 읽었는가?
- [ ] 프론트엔드, 백엔드, API 계약 중 영향 범위를 파악했는가?
- [ ] 사용자 변경과 작업 범위 밖 파일을 보존하고 있는가?

## 공통 구현 규칙

- 기존 아키텍처, 타입, 명명, 포맷터와 패턴을 우선한다.
- 요구사항에 필요한 범위만 변경하고 무관한 리팩터링을 섞지 않는다.
- 비즈니스 규칙을 추측해서 추가하지 않는다.
- 보안정보, 개인정보, 운영 데이터와 로컬 환경 파일을 커밋하지 않는다.
- 기능 변경에는 테스트와 관련 문서를 함께 수정한다.
- 검증 실패나 미실행 항목을 숨기지 않는다.

## 프론트엔드

- 서버를 판단 결과의 원본으로 사용한다.
- 위험도, 적격성, 정책, 증빙 추천과 최종 경로를 화면에서 임의 계산하지 않는다.
- API 계층, 상태 관리, UI 컴포넌트와 표현 로직을 분리한다.
- 로딩, 빈 상태, 오류, 재시도와 접근성을 구현한다.
- API 타입과 enum을 추측하지 않고 실제 계약을 확인한다.
- UI 변경 시 반응형 화면과 주요 상호작용을 검증하고 PR에 증거를 남긴다.

## 백엔드

- API, 서비스, 저장소, 외부 연동과 도메인 규칙을 분리한다.
- 클라이언트 입력을 검증하고 판단값은 서버에서 재계산한다.
- 감사 로그, 버전, 입력 스냅샷, 기준시점과 `demoOnly`를 유지한다.
- 데이터 부재와 위험 악화를 동일하게 취급하지 않는다.
- 의심 데이터 및 미래 시점 데이터는 위험 입력에 사용하지 않는다.
- 외부 연동 실패와 Mock·Fixture·Snapshot·fallback을 응답 및 로그에서 구분한다.
- 스키마 변경은 마이그레이션과 하위 호환성을 검토한다.

## API 계약 변경

API가 바뀌면 다음 항목을 하나의 작업 범위에서 함께 확인한다.

- 백엔드 요청·응답 스키마와 오류 코드
- 프론트엔드 타입, API 클라이언트와 화면 사용처
- Mock·Fixture와 통합 테스트
- API 문서와 예시

필드 삭제, 이름·타입 변경과 enum 축소는 breaking change로 표시한다.
날짜·시간대, null 허용 여부와 HTTP 상태를 양쪽에서 일치시킨다.

## CredAble 안전 원칙

- AI는 서버가 확정한 결과를 설명할 뿐 추천 점수, 위험도, 정책, 경로와 최종 결정을 변경하지 않는다.
- 최종 결정은 심사역에게 남겨둔다.
- 실제 검증 전에는 성능이나 금융 효과를 사실처럼 생성하지 않는다.
- 실제 데이터와 Demo·Mock을 명확히 구분한다.

## 검증 기준

- 공통: 포맷, 린트, 타입 검사, `git diff --check`
- 프론트엔드: 단위·컴포넌트 테스트, 빌드, 주요 화면 확인
- 백엔드: 단위·API·도메인 규칙 테스트, DB 변경 확인
- Full stack: 계약 일치와 핵심 사용자 흐름 확인

존재하는 프로젝트 명령을 우선 사용하며 실행하지 못한 검증은 이유와 함께 보고한다.

## 필수 Git 절차

파일을 변경한다면 반드시 다음 순서를 지킨다.

**기능 하나 = 작업 브랜치 하나 = Pull Request 하나**를 원칙으로 한다.
서로 다른 기능이나 수정 목적을 한 브랜치 또는 PR에 함께 넣지 않는다.
동일 기능에 필요한 프론트엔드·백엔드·API 계약 변경은 하나의 기능 브랜치와 PR에서 함께 처리할 수 있다.

**브랜치 생성 → 구현 → 검증 → 커밋 → 푸시 → PR 생성**

- `main`에 직접 커밋하거나 푸시하지 않는다.
- 변경은 의미 있는 작은 단위로 커밋한다.
- PR에는 변경 이유, 프론트·백엔드 영향, 검증 결과와 위험을 작성한다.
- 브랜치·커밋·PR 중 하나라도 빠지면 완료라고 보고하지 않는다.
- 인증이나 권한 문제로 푸시·PR 생성이 불가능하면 남은 명령을 사용자에게 제공한다.

세부 Git 규칙은 [`CONTRIBUTING.md`](CONTRIBUTING.md)를 따른다.

## 완료 응답

변경 영역, 핵심 내용, 검증 결과, 제한사항, 브랜치명, 커밋 해시와 PR 링크를 보고한다.

