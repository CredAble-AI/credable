# API provider mode: "mock" (default) or "live"
# mock: use the built-in fixture providers, no backend required
# live: call the backend APIs directly
# Any unset or unrecognized value falls back to "mock".
VITE_API_MODE=mock
